name: Admin Panel CI

# Lives under admin/.github/workflows/ in the propmainrepo monorepo, where
# GitHub doesn't read it (only root .github/workflows/ counts there — see
# admin-ci.yml). It becomes live once `git subtree split --prefix=admin`
# lands this at the root of the standalone propadmin repo.

on:
  push:
    branches: [main]
  pull_request: {}
  workflow_dispatch: {}

jobs:
  lint-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check and build
        run: npm run build
